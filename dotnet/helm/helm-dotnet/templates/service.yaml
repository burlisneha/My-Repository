apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-dotnet.fullname" . }}
  labels:
    {{- include "helm-dotnet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-dotnet.selectorLabels" . | nindent 4 }}
